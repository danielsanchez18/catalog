---
import ServiceTable from '@/components/dashboard/ServiceTable';
import { getServices } from '@/lib/db/services';

const services = await getServices(Astro.cookies);
---

<ServiceTable services={services} client:load />