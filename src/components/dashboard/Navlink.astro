---
import { Button } from '@/components/ui/button';
---

<nav class="w-full border-b border-border">
    <div class="w-full max-w-7xl mx-auto px-5 py-2 flex items-center gap-x-1">
        <Button variant="ghost" className="h-fit py-1.5">
            Overview
        </Button>
        <Button variant="ghost" className="h-fit py-1.5">
            Productos
        </Button>
        <Button variant="ghost" className="h-fit py-1.5">
            Servicios
        </Button>
        <Button variant="ghost" className="h-fit py-1.5">
            Clientes
        </Button>
        
    </div>
</nav>